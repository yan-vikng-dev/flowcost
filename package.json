{
  "name": "saas-kit",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "setup": "pnpm i && pnpm run build:data-ops",
    "build:data-ops": "pnpm run --filter data-ops build",
    "dev:user-app": "pnpm run --filter user-app dev",
    "deploy:user-app": "pnpm run build:data-ops && pnpm run --filter user-app deploy",
    "dev:data-service": "pnpm run --filter data-service dev",
    "deploy:data-service": "pnpm run build:data-ops && pnpm run --filter data-service deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "@types/node": "^24.7.2",
    "typescript": "^5.9.3"
  }
}
